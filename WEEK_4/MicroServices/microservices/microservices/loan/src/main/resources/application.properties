spring.application.name=loan

# Account service already occupies the default port 8080, so this
# service is explicitly moved to 8081. Without this line, starting
# "loan" while "account" is already running fails with:
#   Web server failed to start. Port 8080 was already in use.
server.port=8081
